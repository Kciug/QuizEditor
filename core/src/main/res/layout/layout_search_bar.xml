<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <EditText
        android:id="@+id/search_field"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:autofillHints="name"
        android:background="@drawable/background_tile"
        android:drawableStart="@drawable/ic_search_24"
        android:drawableTint="?attr/colorPrimary"
        android:hint="Szukaj"
        android:inputType="text"
        android:paddingHorizontal="10dp"
        android:paddingVertical="5dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageButton
        android:id="@+id/button_clear"
        android:layout_width="wrap_content"
        android:layout_height="34dp"
        android:backgroundTint="@color/transparent"
        android:contentDescription="Wyczyść wyszukiwanie"
        android:src="@drawable/ic_close_24"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/search_field"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/search_field"
        app:tint="?attr/colorPrimary" />

    <TextView
        android:id="@+id/tv_elements_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/text_placeholder_number"
        android:textSize="11sp"
        android:textColor="@color/text_disabled"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/tv_label_elements_count"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintHorizontal_chainStyle="packed"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/search_field"
        app:layout_constraintVertical_bias="0.5" />

    <TextView
        android:id="@+id/tv_label_elements_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="3dp"
        android:text="@string/text_placeholder_short"
        android:textSize="11sp"
        android:textColor="@color/text_disabled"
        app:layout_constraintBottom_toBottomOf="@id/tv_elements_count"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toEndOf="@+id/tv_elements_count"
        app:layout_constraintTop_toTopOf="@id/tv_elements_count" />

</androidx.constraintlayout.widget.ConstraintLayout>